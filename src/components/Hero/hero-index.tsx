import React from 'react'
import { Container } from '../../styles'
import { Form, HeroTitle } from './hero-styles'

const Hero = () => (
  <Form>
    <Container>
      <HeroTitle>
        Vagas de emprego na área de tecnologia, design e sistemas.
      </HeroTitle>
    </Container>
  </Form>
)

export default Hero
